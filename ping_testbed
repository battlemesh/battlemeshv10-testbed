#!/bin/sh

FILE="${1:-./routers}"
logger -s "using file '$FILE'"

LIST_FAILED=

while read -r LINE
do
	IP="$LINE"
	ping -c 1 $IP || LIST_FAILED="$LIST_FAILED $IP"
done <"$FILE"

logger -s "failed IP's: '$LIST_FAILED'"
